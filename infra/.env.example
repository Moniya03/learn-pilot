# ── Postgres ──
POSTGRES_USER=learnpilot
POSTGRES_PASSWORD=learnpilot

# ── Zitadel ──
ZITADEL_ADMIN_USERNAME=zitadel-admin
ZITADEL_ADMIN_PASSWORD=Password1!
# Generate with: openssl rand -hex 16 (must be exactly 32 chars, NOT base64)
ZITADEL_MASTERKEY=

# ── Google OAuth (for Zitadel IdP) ──
# Get from https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# ── RabbitMQ ──
RABBITMQ_USER=learnpilot
RABBITMQ_PASS=learnpilot

# ── MinIO ──
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin

# ── AI keys ──
GROQ_API_KEY=
HF_API_TOKEN=
